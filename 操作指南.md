# 个人收支记账系统 操作指南

## 一、系统概述

个人收支记账系统是一款基于Java + SpringBoot + H5开发的轻量级记账应用，帮助用户管理个人收支情况，支持记录查询、统计分析等功能。

---

## 二、环境要求

- JDK 8 或以上版本
- Maven 3.6+（用于构建项目）
- 现代浏览器（Chrome、Firefox、Safari等，支持H5）

---

## 三、项目启动步骤

### 方式一：使用Maven启动（推荐）

1. **确保已安装JDK和Maven**
   - 打开命令提示符，执行 `java -version` 检查Java版本
   - 执行 `mvn -version` 检查Maven版本

2. **进入项目目录**
   ```bash
   cd D:\workspace\traeTest\trae0319\trae0319_dogFooding\trea0319
   ```

3. **构建并启动项目**
   ```bash
   mvn spring-boot:run
   ```

4. **访问系统**
   打开浏览器访问：`http://localhost:8080`

### 方式二：使用IDEA或其他IDE启动

1. 打开IDEA，选择"Open"项目
2. 选择项目根目录（包含pom.xml的文件夹）
3. 等待Maven依赖下载完成
4. 找到 `src/main/java/com/example/accounting/AccountingSystemApplication.java`
5. 右键点击，选择"Run AccountingSystemApplication"

### 方式三：打包成Jar运行

1. 执行打包命令
   ```bash
   mvn clean package -DskipTests
   ```

2. 在 `target` 目录下找到 `accounting-system-1.0.0.jar`

3. 运行Jar包
   ```bash
   java -jar target/accounting-system-1.0.0.jar
   ```

---

## 四、前端页面操作指南

### 1. 注册账号

1. 打开系统首页，点击"立即注册"
2. 填写注册信息：
   - 用户名：3-20个字符
   - 密码：6-20个字符
   - 手机号（选填）：用于登录
   - 邮箱（选填）：用于登录
3. 点击"注册"按钮完成注册

### 2. 登录系统

1. 在登录页面输入账号（用户名/手机号/邮箱均可）
2. 输入密码
3. 点击"登录"按钮

### 3. 记账功能

1. 登录后点击底部"记账"标签
2. 选择"收入"或"支出"类型
3. 输入金额（必须大于0）
4. 选择分类（如餐饮、薪资、购物等）
5. 可选：填写备注信息
6. 点击"保存"按钮完成记账

### 4. 查看账单明细

1. 点击底部"账单"标签
2. 可通过顶部筛选栏选择"全部"、"收入"、"支出"
3. 点击任意记录可进入编辑模式

### 5. 编辑/删除记录

1. 在账单列表中点击要修改的记录
2. 在弹出的编辑窗口中修改信息
3. 点击"保存"更新记录，或点击"删除"删除该记录

### 6. 统计分析

1. 点击底部"统计"标签
2. 可选择"本周"或"本月"统计
3. 查看收支总览和分类占比饼图

### 7. 个人中心

1. 点击底部"我的"标签
2. 查看个人信息
3. 可修改密码或退出登录

---

## 五、接口测试指南

### 使用Postman测试

1. **注册接口**
   - URL：`POST http://localhost:8080/api/user/register`
   - Body（raw JSON）：
     ```json
     {
         "username": "testuser",
         "password": "123456",
         "phone": "13800138000"
     }
     ```

2. **登录接口**
   - URL：`POST http://localhost:8080/api/user/login`
   - Body（raw JSON）：
     ```json
     {
         "account": "testuser",
         "password": "123456"
     }
     ```
   - 从响应中获取 `token`

3. **添加记录接口（需要登录）**
   - URL：`POST http://localhost:8080/api/record`
   - Headers：添加 `Authorization: Bearer {token}`
   - Body（raw JSON）：
     ```json
     {
         "amount": 50.00,
         "type": "支出",
         "category": "餐饮",
         "remark": "午餐"
     }
     ```

4. **查询记录接口**
   - URL：`GET http://localhost:8080/api/record?page=1&pageSize=10`
   - Headers：添加 `Authorization: Bearer {token}`

5. **统计接口**
   - URL：`GET http://localhost:8080/api/statistics/month`
   - Headers：添加 `Authorization: Bearer {token}`

---

## 六、数据文件说明

系统启动后会在项目根目录创建 `data` 文件夹，包含两个CSV文件：

- `users.csv`：存储用户信息
- `records.csv`：存储收支记录

文件格式说明：

**users.csv**
| id | username | password | phone | email | createTime |
|----|----------|----------|-------|-------|------------|
| 1 | testuser | e10adc3949ba59abbe56e057f20f883e | 13800138000 | | 2024-01-01 12:00:00 |

*注意：密码使用MD5加密存储*

**records.csv**
| id | userId | amount | type | category | remark | createTime |
|----|--------|--------|------|----------|--------|------------|
| 1 | 1 | 50.00 | 支出 | 餐饮 | 午餐 | 2024-01-01 12:30:00 |

---

## 七、常见问题

### Q1：启动项目时提示"Maven不是内部或外部命令"
A：请确认已安装Maven并配置了环境变量，或使用IDE方式启动。

### Q2：项目启动成功但无法访问页面
A：请检查：
1. 端口8080是否被占用
2. 浏览器访问地址是否正确（http://localhost:8080）
3. 查看控制台是否有错误信息

### Q3：忘记密码怎么办
A：目前系统没有找回密码功能，可以：
1. 使用其他账号登录
2. 或删除 `data/users.csv` 文件重新注册

### Q4：如何修改系统端口
A：修改 `src/main/resources/application.properties` 中的 `server.port` 属性

### Q5：数据存在哪里，可以备份吗？
A：数据存储在项目根目录下的 `data` 文件夹中，可以直接备份该文件夹。

---

## 八、功能特点总结

✅ **用户管理**：注册、登录、修改密码  
✅ **记账功能**：添加、编辑、删除收支记录  
✅ **查询功能**：按时间、类型、分类筛选  
✅ **统计分析**：周/月统计、分类占比饼图  
✅ **数据持久化**：CSV文件存储，无需数据库  
✅ **响应式设计**：支持手机、平板等移动设备  
✅ **接口安全**：JWT令牌认证  
✅ **参数校验**：完整的输入验证机制
